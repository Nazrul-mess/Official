
// JavaScript to dynamically count scores
const memberCount = 22; // Current number of members
const locationCount = 1; // Current number of locations
const studentCount = 53; // Current number of enrolled students

document.getElementById('memberCount').textContent = memberCount;
document.getElementById('locationCount').textContent = locationCount;
document.getElementById('studentCount').textContent = studentCount;
